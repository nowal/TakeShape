const IntegratePage = () => (
  <main className="mx-auto w-full max-w-2xl px-4 py-12 sm:px-6">
    <h1 className="text-3xl font-semibold">CRM integrations paused</h1>
    <p className="mt-2 text-sm text-slate-600">
      This legacy integration page is not part of the current provider referral
      dashboard launch.
    </p>
  </main>
);

export default IntegratePage;
