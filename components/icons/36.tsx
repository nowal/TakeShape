import type {FC} from 'react';
import {CommonIcon, TCommonIconProps} from '@/components/icons';

export const CommonIcon36: FC<TCommonIconProps> = (props) => {
  return <CommonIcon size={36} viewBox="0 0 36 36" {...props} />;
};
