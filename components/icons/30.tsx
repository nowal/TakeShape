import type {FC} from 'react';
import {CommonIcon, TCommonIconProps} from '@/components/icons';

export const CommonIcon30: FC<TCommonIconProps> = (props) => {
  return <CommonIcon size={30} {...props} />;
};
