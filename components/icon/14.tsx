import { TCommonIconProps, CommonIcon } from '@/components/icon';
import type {FC} from 'react';

export const CommonIcon14: FC<TCommonIconProps> = (props) => {
  return <CommonIcon size={14} viewBox="0 0 14 14" {...props} />;
};
