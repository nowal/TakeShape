import type {FC} from 'react';
import {CommonIcon, TCommonIconProps} from '@/components/icon';

export const CommonIcon24: FC<TCommonIconProps> = (props) => {
  return <CommonIcon size={24} viewBox="0 0 24 24" {...props} />;
};
