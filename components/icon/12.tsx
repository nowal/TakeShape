import { TCommonIconProps, CommonIcon } from '@/components/icon';
import type {FC} from 'react';

export const CommonIcon12: FC<TCommonIconProps> = (props) => {
  return <CommonIcon size={12} viewBox="0 0 12 12" {...props} />;
};
