import type {FC} from 'react';
import {CommonIcon, TCommonIconProps} from '@/components/icon';

export const CommonIcon18: FC<TCommonIconProps> = (props) => {
  return <CommonIcon size={18} viewBox="0 0 18 18" {...props} />;
};
