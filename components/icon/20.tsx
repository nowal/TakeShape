import type {FC} from 'react';
import {CommonIcon, TCommonIconProps} from '@/components/icon';

export const CommonIcon20: FC<TCommonIconProps> = (props) => {
  return <CommonIcon size={20} viewBox="0 0 20 20" {...props} />;
};
