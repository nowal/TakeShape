import type {FC} from 'react';
import {CommonIcon, TCommonIconProps} from '@/components/icon';

export const CommonIcon30vb24: FC<TCommonIconProps> = (props) => {
  return <CommonIcon size={30} {...props} />;
};
